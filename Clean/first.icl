module first  // 1st line module name always
import StdEnv // second line import always

//module name should always be the same as file name (no spaces)

//Start = "Hello World" // prints hello world


// how a clean function looks like
//functionName :: typeofInputParameter -> typeofOutputParameter
//functionName variablename = code


Square :: Int -> Int
Square x = x * x      // * is the multiplication operator

Start = Square 3 // result = 9

